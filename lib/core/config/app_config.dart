class AppConfig {
  static const mockCredentials = {
    'email': 'test@example.com',
    'password': 'password123'
  };

  // Feature flags
  static const bool useMockLogin = true; // Always enabled
}
